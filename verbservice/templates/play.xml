<?xml version="1.0" encoding="UTF-8"?>
<Response>
    <Play {{#loop}} loop="{{loop}}"{{/loop}}{{#digits}} 
        digits="{{digits}}" {{/digits}}> {{#url}}
        {{url}}{{/url}}
    </Play>
</Response>
